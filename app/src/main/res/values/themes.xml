<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyTest" parent="Base.Theme.MyTest" />

    <!-- 透明页面主题 -->
    <style name="TranslucentTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">#00000000</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="BasicLayoutMatch">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="BasicLayoutWrap">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!--welcomeTvBasicStyle-->
    <style name="welcomeBasicTvStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/x120</item>
        <item name="android:textSize">@dimen/x55</item>
        <item name="android:textStyle">bold</item>
        <item name="paddingStart">@dimen/x20</item>
        <item name="android:gravity">center</item>
    </style>
</resources>